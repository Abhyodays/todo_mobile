import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container:{
        marginBottom:30
    },
    text:{
        fontSize: 20,
        width: '80%'
    }
})

export default styles;